{
    "name": "ts-start-cf-workflow-mono",
    "private": true,
    "version": "0.0.0",
    "packageManager": "bun@1.1.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo dev",
        "build": "turbo build",
        "lint": "turbo lint",
        "typecheck": "turbo typecheck",
        "test": "turbo test",
        "clean": "turbo clean && rm -rf node_modules **/node_modules .turbo"
    },
    "devDependencies": {
        "turbo": "^2.0.0",
        "typescript": "^5.5.0"
    }
}